# WakaRoute iOS へのコントリビューション

WakaRoute に関心を持っていただき、ありがとうございます。小さな文言修正から機能提案まで歓迎します。

Web アプリ側は [wakaroute-web](https://github.com/Receipt-Roller/wakaroute-web) です。

## 進め方

1. 既存の Issue を検索し、同じ内容がないか確認してください。
2. 大きな変更は、実装前に Issue で目的と設計を相談してください。
3. Fork から作業ブランチを作成し、**変更理由が分かる** Pull Request を送ってください。
4. UI を変えた場合は、**iPhone と iPad の両方**で確認した内容を記載してください。

## 開発と検証

```bash
cd WakaRouteKit && swift test
```

```bash
xcodebuild -project WakaRoute.xcodeproj -scheme WakaRoute \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
```

ロジックは `WakaRouteKit` に置いてください。アプリターゲットは画面だけです。そうしておくと、シミュレータを起動せずにテストが走ります。

## このプロジェクトで大事にしていること

- **理由をコメントに書く。** 何をしているかはコードが語ります。書くべきは、なぜそうしたか、なぜ別のやり方にしなかったかです。
- **数字をでっち上げない。** 根拠のない理解度や点数は出しません。分からないときは「分からない」と表示します。
- **生徒の作業を失わない。** 送信できないものは端末に貯めて再送します。捨てる場合は、捨てたことを本人に伝えます。
- **未成年から集める情報は最小限に。** 機能に本当に必要なものだけです。
- **端末ではなくサイズで判断する。** `if iPad` は書きません。Split View と Stage Manager で破綻します。

詳しくは [AGENTS.md](AGENTS.md) を読んでください。特に「知らないと必ず踏むバックエンドの挙動」の節は、変更前に目を通していただけると事故が減ります。

## 公開リポジトリであることについて

**実在するアカウントID、トークン、本番から取得したレスポンスをそのままコミットしないでください。** テストのフィクスチャは、見て明らかに架空と分かる値にしてください。

## 報告

脆弱性や個人情報に関わる問題は、Issue ではなく [SECURITY.md](SECURITY.md) の手順でお願いします。
